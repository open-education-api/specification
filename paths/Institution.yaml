get:
  summary: GET /
  description: Get institution data of provider of this service.
  tags:
    - institution metadata
  responses:
    '200':
      description: OK
      schema:
        $ref: '../definitions/Instituion.yaml'
