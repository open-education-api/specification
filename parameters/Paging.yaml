name: pageSize
in: query
description: The number of items per page
required: false
schema:
  type: integer
  format: int32
  default: 10
  enum:
    - 10
    - 20
    - 50
    - 100
    - 250
name: pageNumber
in: query
description: The page number to get
required: false
schema:
  type: integer
  format: int32
  default: 1
