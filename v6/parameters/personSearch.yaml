name: q
in: query
description: |
  Filter by persons having a givenName, surNamePrefix, surname, displayName, initials, email
  or secondaryEmail containing the given search term (exact partial match, case insensitive)
required: false
schema:
  type: string
