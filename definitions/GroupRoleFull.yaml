allOf:
  - type: object
    required:
      - grouproleId
    properties:
      grouproleId:
        type: string
        description: Unique id for this grouprole
  - $ref: 'GroupRole.yaml'
  - type: object
    required:
      - lastModified
    properties:
      lastModified:
        type: string
        description: The timestamp for the last moment this grouprole was updated
        format: date-time
