allOf:
  - $ref: '../schemas/ProgramId.yaml'
  - $ref: '../schemas/ProgramProperties.yaml'
  - type: object
    description: container for consumers
    properties:
      consumers:
        description: The additional consumer elements that can be provided
        type: array
        items:
          $ref: '../schemas/Consumer.yaml'
  - type: object 
    description: time override element
    properties:
      timeOverride:
        description: time override element
        type: array
        items:
        - $ref: '../schemas/TimeOverRideProgram.yaml'
