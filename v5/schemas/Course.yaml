allOf:
  - $ref: '../schemas/CourseId.yaml'
  - $ref: '../schemas/CourseProperties.yaml'
  - type: object
    description: container for consumers
    properties:
      consumers:
        description: The additional consumer elements that can be provided
        type: array
        items:
          $ref: './Consumer.yaml'
  - type: object
    description: time override element
    properties:
      timeOverride:
        description: time override element
        type: array
        items:
        - $ref: '../schemas/TimeOverRideCourse.yaml'
