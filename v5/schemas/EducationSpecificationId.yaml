type: object
properties:
  educationSpecificationId:
    type: string
    description: Unique id for this education specification. This id should be a stable (persistent) uuid, always pointing to the same resource.
    format: uuid
    example: 123e4567-e89b-12d3-a456-426614174000
required:
  - educationSpecificationId
