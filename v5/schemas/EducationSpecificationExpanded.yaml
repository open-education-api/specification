allOf:
  - $ref: '../schemas/EducationSpecification.yaml'
  - type: object
    title: With timelineOverrides
    properties:
      timelineOverrides:
        description: Timeline overrides allow an implementation to provide versions of entities that will be valid in the future or have been in the past. The `timelineOverrides` attribute should only be returned when the query parameter `returnTimelineOverrides` is `true`.
        type: array
        items:
          $ref: '../schemas/TimelineOverrideEducationSpecification.yaml'