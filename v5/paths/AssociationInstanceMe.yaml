post:
  summary: POST /associations/me
  description: POST a single association (enroll a person based on person information obtained from .wellknown endpoint and offering object)
  security:
    - openId:
      - my_association_write
  tags:
    - associations

  requestBody:
    required: true
    content:
      application/json:
        schema:  
          type: object
          properties:
            issuer:
              $ref: '../schemas/Organization.yaml'            
            associationItem:
              $ref: '../schemas/AssociationProp.yaml'
            offering:
              $ref: '../schemas/Offering.yaml'
  responses:
    '201':
      description: OK
      content:
        application/json:
          schema:
            properties:
              response:
                allOf:
                  - $ref: '../schemas/AssociationId.yaml'
                  - $ref: '../schemas/PostResponse.yaml'

    '400':
      description: Bad Request
      content:
        application/problem+json:
          schema:
            $ref: '../schemas/Problem.yaml'