allOf:
  - $ref: './Room.yaml'
  - type: object
    description: These properties are only present when explicitly included.
    properties:
      building:
        description: The building in which the room is located
        $ref: './Building.yaml'
