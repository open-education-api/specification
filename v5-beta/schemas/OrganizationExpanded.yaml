allOf:
  - $ref: './Organization.yaml'
  - type: object
    description: These properties are only present when explicitly included
    properties:
      parent:
        allOf: # Use allOf to make sure description renders
          - $ref: './Organization.yaml'
          - description: "The organizational unit which is the parent of this organization"
      children:
        type: array
        description: "All the organizational units for which this organization is the parent"
        items:
          $ref: './Organization.yaml'
