type: object
description: A time-line override of the program.
properties:
  startDate:
    type: string
    description: The day on which this timelineOverride starts, RFC3339 (date)
    format: date
    example: 2021-09-01
  endDate:
    type: string
    description: The day on which this timelineOverride ends, RFC3339 (date)
    format: date
    example: 2022-08-31
  program:
    $ref: './ProgramProperties.yaml'
