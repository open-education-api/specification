allOf:
  - $ref: './Component.yaml'
  - type: object
    description: These properties are only present when explicitly included
    properties:
      course:
        description: The course of which this component is a part
        $ref: './Course.yaml'
      organization:
        description: Organization which provides this component
        $ref: './Organization.yaml'
