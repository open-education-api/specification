allOf:
  - $ref: './Program.yaml'
  - type: object
    description: These properties are only present when explicitly included
    properties:
      parent:
        $ref: './Program.yaml'
      children:
        type: array
        items:
          $ref: './Program.yaml'
