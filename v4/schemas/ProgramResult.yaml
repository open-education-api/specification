allOf:
  - $ref: './Result.yaml'
  - type: object
    required:
      - ects
    properties:
      ects:
        type: number
        description: The number of EC's that have been earned to the current moment
        minimum: 0
        example: 15
